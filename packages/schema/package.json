{
  "name": "@getlibero/schema",
  "version": "0.0.1",
  "private": true,
  "description": "Zod schemas for Libero: team sheets, audit records, tool calls, approval requests, memory ops. Single source of truth — both services import from here.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "smol-toml": "^1.7.1"
  }
}
