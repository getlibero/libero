{
  "name": "@getlibero/agent",
  "version": "0.0.1",
  "private": true,
  "description": "Agent loop for Libero: provider-agnostic completion layer and ReAct loop with loop-side caps. Reaches tools only over the network.",
  "license": "MIT",
  "files": ["dist"],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude '**/dist/**'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "@getlibero/schema": "workspace:*",
    "openai": "^7.3.0"
  }
}
