{
  "name": "@getlibero/gateway",
  "version": "0.0.1",
  "private": true,
  "description": "Slack gateway for Libero: Socket Mode adapter, mention intake, approval cards.",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test --test-reporter=@getlibero/test-kit/reporter 'dist/**/*.test.js'"
  },
  "dependencies": {
    "@getlibero/schema": "workspace:*",
    "@slack/logger": "^5.0.0",
    "@slack/socket-mode": "^3.0.1",
    "@slack/types": "^3.1.0",
    "@slack/web-api": "^8.1.1",
    "undici": "^8.10.0"
  },
  "devDependencies": {
    "@getlibero/test-kit": "workspace:*",
    "@types/node": "^24.13.3",
    "expect": "^30.5.0"
  }
}
