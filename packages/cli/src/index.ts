#!/usr/bin/env node
// @getlibero/cli — placeholder release (defensive namespace claim).
// Commands land in Phase 0–1: init, channel add, audit, budget reset, doctor.
console.log("libero: this is a placeholder release. Follow https://github.com/getlibero/libero");
